/* Escrever um programa para exibir os números de 1 até 50 na tela. */


const numeros = () => {
    numero = 1
    while (numero <= 50) {
        console.log(`Número: ${numero}`)
        numero++
    }
}

numeros()